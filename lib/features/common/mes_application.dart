import 'package:flutter/material.dart';

class MesApplication extends StatelessWidget {
  const MesApplication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
